{
  "name": "tenders-agent",
  "version": "1.0.0",
  "description": "A basic agent that retrieves public tender data from an Airtable database via the Airtable MCP",
  "main": "agent.js",
  "type": "module",
  "scripts": {
    "start": "node agent.js"
  },
  "dependencies": {
    "@airtable/mcp-cli": "^0.2.4",
    "@openai/agents": "^0.11.5",
    "dotenv": "^16.6.1"
  }
}
